# ── WanderNest — Backend (Railway Service: root dir = server/) ──
# Railpack/Nixpacks picks this up when root directory is set to "server"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[start]
cmd = "node src/server.js"
